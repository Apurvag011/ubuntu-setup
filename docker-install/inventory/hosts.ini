[docker_hosts]
localhost ansible_connection=local

[docker_hosts:vars]
# Uncomment and set if targeting a remote host instead of localhost:
# ansible_ssh_private_key_file=~/.ssh/id_rsa
# ansible_become_password=your_sudo_password
