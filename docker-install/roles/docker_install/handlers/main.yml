---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  listen: Reload systemd

- name: Restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
  listen: Restart docker
